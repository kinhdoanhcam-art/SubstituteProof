Frontend dependency-build attempt in packaging environment
=========================================================

Command attempted:
  npm install --no-audit --no-fund

Result:
  NOT COMPLETED because the environment could not resolve registry.npmjs.org.
  npm log reported EAI_AGAIN while fetching genlayer-js@1.1.8 (and vite during npx probe).

What was verified instead:
  - TypeScript 5.8.3 transpile syntax PASS for src/App.tsx, src/main.tsx,
    src/genlayer.ts, src/config.ts, src/types.ts.
  - Production contract/local executable gates rerun and PASS.

Do not claim a dependency-installed Vite build until GitHub/Vercel completes npm install and npm run build.
